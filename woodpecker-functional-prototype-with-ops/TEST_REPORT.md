# Functional Prototype Test Report

测试日期：2026-08-05

## 静态检查

| 检查项 | 结果 |
|---|---|
| `data.js` 语法检查 | 通过 |
| `ui.js` 语法检查 | 通过 |
| `views.js` 语法检查 | 通过 |
| `app.js` 语法检查 | 通过 |
| CSS 解析 | 通过，612 个顶层规则，0 个解析错误 |
| 外部运行时依赖 | 无 |

## 路由渲染检查

- 实际 Router 匹配规则：60 条
- 自动访问的页面/标签状态：67 个
- JavaScript 页面错误：0
- 浏览器 Console Error：0
- 误入 404 的已登记页面：0
- 404 fallback：保留并可用

覆盖范围包括：

- 登录、Overview、仓库、分支、PR、流水线、日志和设置
- 组织、管理员、Agent、Queue、Forge 和个人设置
- 基础设施、服务器、服务器详情全部 6 个标签、服务器组、服务和告警
- 部署中心、部署向导、部署详情、审批、应用、环境、Release 和策略

## 业务交互检查

以下流程已通过浏览器自动化测试：

1. 打开 `prod-api-01` 服务器详情。
2. 切换服务器进入维护模式，并验证按钮变为“退出维护”。
3. 打开告警中心并确认活动告警。
4. 注册新服务器 `prod-api-04`，验证数据写入并跳转服务器详情。
5. 从 `backend-api` 应用发起部署。
6. 在部署向导中显式选择 Production。
7. 完成服务器组、策略、预检查与确认步骤。
8. 提交生产审批。
9. 批准部署并启动逐节点状态流转。
10. 验证发布进度与部署日志持续更新。
11. 在异常/维护目标导致失败时，验证回滚确认入口。
12. 创建服务器组、应用与环境，并验证列表状态更新。
13. 切换部署策略启用状态。
14. 重启服务实例并验证反馈。
15. 暂停和继续运行中的 Deployment。

测试期间页面错误和 Console Error 均为 0。

## 响应式检查

在 `390 × 844` 手机视口下检查：

- 基础设施概览
- 服务器列表
- 服务器详情概览
- 服务器监控
- 部署中心
- 新建部署
- Deployment 详情
- 应用列表
- 环境列表

结果：

- 所有页面正常渲染
- 无页面级横向溢出
- 宽表格限制在内部滚动容器
- 侧边栏使用移动端抽屉模式
- 顶部命令搜索和用户控件不会撑宽视口

## 原型限制

测试验证的是浏览器内的产品行为和状态流转，不代表真实基础设施操作：

- 未连接 Woodpecker Server API
- 未连接真实 Node Agent
- 未采集真实 Prometheus 指标
- 未执行 SSH、Docker、Kubernetes 或 systemd 命令
- 未验证 mTLS、任务签名、证书轮换或真实审批服务
